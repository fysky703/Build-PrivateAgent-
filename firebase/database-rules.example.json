{
  "rules": {
    ".read": false,
    ".write": false,
    "devices": { "$deviceId": { ".read": "auth != null", ".write": "auth != null" } },
    "commands": { "$deviceId": { ".read": "auth != null", ".write": "auth != null" } },
    "responses": { "$deviceId": { ".read": "auth != null", ".write": "auth != null" } }
  }
}
